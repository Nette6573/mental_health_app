// frontend/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: Ensures trailing slashes for consistent routing on Pages
  // trailingSlash: true,
};

module.exports = nextConfig;